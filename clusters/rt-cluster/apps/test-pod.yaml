apiVersion: v1
kind: Pod
metadata:
  name: flux-test-pod
  namespace: default
spec:
  containers:
  - name: busybox
    image: busybox
    command: ["sh", "-c", "while true; do echo 'Flux is working'; sleep 3600; done"]
